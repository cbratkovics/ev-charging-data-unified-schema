# Model card — PLACEHOLDER

This file is generated by `python scripts/evaluate.py` from `artifacts/eval/*.json` and
`artifacts/models/<version>/metadata.json`. Until then it carries no numbers, and
`scripts/check_model_card.py` fails so nothing unbacked can be published.

| Metric | Value |
|---|---|
| MAE | TBD |
