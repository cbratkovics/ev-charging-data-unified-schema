# Source contracts

_Generated by `scripts/render_contracts.py` from `ev_charging_data_unified_schema/data/contracts.py`. Do not edit by hand; `--check` runs in CI._

A contract declares, per source and per file family, the columns the published file is expected to carry, the logical type of their values (everything lands as strings; the type is what the values parse as), whether each column is required, the alias map for renamed columns, the literal null tokens and the published duration unit. The drift policy that uses them is in docs/adr/0008-drift-policy.md.

## `boulder`

- station timezone: `America/Denver`
- user-level columns (never selected by silver): none

#### Family `transactions`

- file pattern: `Electric_Vehicle_Charging_Station_Data`
- aliases (published to canonical): none
- null tokens: none
- published duration unit: no duration column

| Column | Logical type | Required | Description |
|---|---|---|---|
| `Station_Name` | text | yes | station display name; the only station identifier |
| `Address` | text | no | station street address (site key) |
| `City` | text | no | station city |
| `State_Province` | text | no | station state |
| `Zip_Postal_Code` | text | no | station postal code |
| `Start_Date___Time` | text | yes | session start, wall-clock local, two formats |
| `Start_Time_Zone` | text | no | publisher's zone label; unreliable per row (docs/PROFILE.md) |
| `End_Date___Time` | text | yes | session end, wall-clock local, two formats |
| `End_Time_Zone` | text | no | publisher's zone label |
| `Total_Duration__hh_mm_ss_` | duration_hms | yes | plug-in duration hh:mm:ss, hours unbounded |
| `Charging_Time__hh_mm_ss_` | duration_hms | yes | charging duration hh:mm:ss |
| `Energy__kWh_` | decimal | yes | energy delivered, kWh |
| `GHG_Savings__kg_` | decimal | no | publisher's derived estimate; not used |
| `Gasoline_Savings__gallons_` | decimal | no | publisher's derived estimate; not used |
| `Port_Type` | text | no | port class (Level 2 throughout) |
| `ObjectID` | integer | yes | per-delivery row id; restarts at 0 at each delivery |
| `ObjectId2` | integer | yes | row id over the whole file |

## `cary`

- station timezone: `America/New_York`
- user-level columns (never selected by silver): none

#### Family `sessions`

- file pattern: `electric-vehicle-charging-stations`
- aliases (published to canonical): none
- null tokens: none
- published duration unit: no duration column

| Column | Logical type | Required | Description |
|---|---|---|---|
| `start_date` | datetime | yes | session start, ISO 8601 with +00:00 (true UTC, ADR-0005 c) |
| `station_name` | text | yes | station display name; the only station identifier |
| `charging_time_hh_mm_ss` | duration_hms | yes | charging duration hh:mm:ss |
| `energy_kwh` | decimal | yes | energy delivered, kWh |
| `address_1` | text | no | station street address |
| `address_2` | text | no | station address line 2 |
| `city` | text | no | station city |
| `state_province` | text | no | station state |
| `zip_postal_code` | text | no | station postal code |

## `dft_2017`

- station timezone: `Europe/London`
- user-level columns (never selected by silver): none

#### Family `rapids`

- file pattern: `local_authority_rapids_raw`
- aliases (published to canonical): `EnergySupplied` to `Energy`
- null tokens: `NA`
- published duration unit: minutes

| Column | Logical type | Required | Description |
|---|---|---|---|
| `ChargingEvent` | text | no | publisher's event id: an integer or a UUID; null or repeated in the fasts files |
| `CPID` | text | yes | charge-point id; null on some fasts rows |
| `Connector` | text | no | connector id 1..3, sometimes a text variant, often null |
| `StartDate` | date | yes | start date; ISO or day-first by family |
| `StartTime` | duration_hms | yes | start time of day hh:mm:ss |
| `EndDate` | date | yes | end date |
| `EndTime` | duration_hms | yes | end time of day hh:mm:ss |
| `Energy` | decimal | yes | energy delivered, kWh (published as EnergySupplied in the rapids files) |
| `Name` | text | yes | funding body (a council or public-sector organisation); the site key |
| `Price` | text | no | fee charged; NA tokens |
| `PluginDuration` | integer | yes | plug-in duration, minutes |

#### Family `rapids_anomalies`

- file pattern: `local_authority_rapids_incomplete_anomalies`
- aliases (published to canonical): `EnergySupplied` to `Energy`
- null tokens: `NA`
- published duration unit: no duration column

| Column | Logical type | Required | Description |
|---|---|---|---|
| `ChargingEvent` | text | no | publisher's event id: an integer or a UUID; null or repeated in the fasts files |
| `CPID` | text | yes | charge-point id; null on some fasts rows |
| `Connector` | text | no | connector id 1..3, sometimes a text variant, often null |
| `StartDate` | date | yes | start date; ISO or day-first by family |
| `StartTime` | duration_hms | yes | start time of day hh:mm:ss |
| `EndDate` | date | yes | end date |
| `EndTime` | duration_hms | yes | end time of day hh:mm:ss |
| `Energy` | decimal | yes | energy delivered, kWh (published as EnergySupplied in the rapids files) |
| `Name` | text | yes | funding body (a council or public-sector organisation); the site key |
| `Price` | text | no | fee charged; NA tokens |
| `Area code` | text | no | ONS area code of the funding body; NA tokens |

#### Family `fasts`

- file pattern: `public_sector_fasts_raw`
- aliases (published to canonical): none
- null tokens: `NA`
- published duration unit: hours

| Column | Logical type | Required | Description |
|---|---|---|---|
| `publisher_row_index` | integer | no | unnamed leading index column |
| `ChargingEvent` | text | no | publisher's event id: an integer or a UUID; null or repeated in the fasts files |
| `CPID` | text | yes | charge-point id; null on some fasts rows |
| `Connector` | text | no | connector id 1..3, sometimes a text variant, often null |
| `StartDate` | date | yes | start date; ISO or day-first by family |
| `StartTime` | duration_hms | yes | start time of day hh:mm:ss |
| `EndDate` | date | yes | end date |
| `EndTime` | duration_hms | yes | end time of day hh:mm:ss |
| `Energy` | decimal | yes | energy delivered, kWh (published as EnergySupplied in the rapids files) |
| `Name` | text | yes | funding body (a council or public-sector organisation); the site key |
| `PluginDuration` | decimal | yes | plug-in duration, hours |

#### Family `fasts_anomalies`

- file pattern: `public_sector_fasts_incomplete_anomalies`
- aliases (published to canonical): none
- null tokens: `NA`
- published duration unit: hours

| Column | Logical type | Required | Description |
|---|---|---|---|
| `publisher_row_index` | integer | no | unnamed leading index column |
| `ChargingEvent` | text | no | publisher's event id: an integer or a UUID; null or repeated in the fasts files |
| `CPID` | text | yes | charge-point id; null on some fasts rows |
| `Connector` | text | no | connector id 1..3, sometimes a text variant, often null |
| `StartDate` | date | yes | start date; ISO or day-first by family |
| `StartTime` | duration_hms | yes | start time of day hh:mm:ss |
| `EndDate` | date | yes | end date |
| `EndTime` | duration_hms | yes | end time of day hh:mm:ss |
| `Energy` | decimal | yes | energy delivered, kWh (published as EnergySupplied in the rapids files) |
| `Name` | text | yes | funding body (a council or public-sector organisation); the site key |
| `PluginDuration` | decimal | yes | plug-in duration, hours |

### Columns across the `dft_2017` families

| Column | `rapids` | `rapids_anomalies` | `fasts` | `fasts_anomalies` |
|---|---|---|---|---|
| `ChargingEvent` | text (optional) | text (optional) | text (optional) | text (optional) |
| `CPID` | text | text | text | text |
| `Connector` | text (optional) | text (optional) | text (optional) | text (optional) |
| `StartDate` | date | date | date | date |
| `StartTime` | duration_hms | duration_hms | duration_hms | duration_hms |
| `EndDate` | date | date | date | date |
| `EndTime` | duration_hms | duration_hms | duration_hms | duration_hms |
| `Energy` | decimal | decimal | decimal | decimal |
| `Name` | text | text | text | text |
| `Price` | text (optional) | text (optional) | absent | absent |
| `PluginDuration` | integer, minutes | absent | decimal, hours | decimal, hours |
| `Area code` | absent | text (optional) | absent | absent |
| `publisher_row_index` | absent | absent | integer (optional) | integer (optional) |
| `EnergySupplied` | published as `EnergySupplied`, aliased to `Energy` | published as `EnergySupplied`, aliased to `Energy` | absent | absent |
