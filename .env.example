# Copy to .env if you ever need local overrides (git-ignored). The project reads no secret:
# every source is public and downloaded without a key. Optional overrides use the
# EV_CHARGING_DATA_UNIFIED_SCHEMA_ prefix (see ev_charging_data_unified_schema/config.py).
